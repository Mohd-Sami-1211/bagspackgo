import { pageMetadata } from '@/lib/seo';
export const metadata = pageMetadata({ title: 'Privacy Policy', description: 'Read the Bagspackgo privacy policy and how personal information is handled on the travel platform.', path: '/privacy' });
export default function PrivacyLayout({ children }) { return children; }

