import { pageMetadata } from '@/lib/seo';
export const metadata = pageMetadata({ title: 'Terms & Conditions', description: 'Read the terms and conditions for using Bagspackgo and its travel services.', path: '/terms' });
export default function TermsLayout({ children }) { return children; }

