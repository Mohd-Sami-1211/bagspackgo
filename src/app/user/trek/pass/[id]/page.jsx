'use client';
// Trek pass page — reuses the same unified pass component as trips
// The trip pass component already handles trek bookings, so we re-export it here.
export { default } from 'src/app/user/trip/pass/[id]/page';
